/**
 * @param {string} s
 * @return {string}
 */
var reverseWords = function(s) {
    let arr= s.split(" ");
    s=""
    for(let i=arr.length-1;i>=0;i--){
        if(arr[i]!=""&&arr[i]!=" "){
            s += arr[i]+ " ";
        }
    }
    return s.trim();
};
